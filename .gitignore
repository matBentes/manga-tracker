# ── Backend ───────────────────────────────────────────────────────────────────
backend/.gradle/
backend/build/
backend/out/
*.class
*.jar
!gradle/wrapper/gradle-wrapper.jar
!backend/gradle/wrapper/gradle-wrapper.jar

# ── Frontend ──────────────────────────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.angular/
frontend/coverage/

# ── Environment / secrets ─────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── OS / IDE ─────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.iml
.vscode/*
!.vscode/extensions.json

# ── Test artifacts ──────────────────────────────────────────────────────────
frontend/playwright-report/
frontend/test-results/
test-results/

# ── Claude local settings ───────────────────────────────────────────────────
.claude/settings.local.json
**/.claude/settings.local.json

# ── Local agent workflow setup ──────────────────────────────────────────────
.claude/commands/
.claude/skills/
.claude/skills.link-backup
.opencode/

# ── Playwright MCP ──────────────────────────────────────────────────────────
.playwright-mcp/

# ── Local helper scripts ────────────────────────────────────────────────────
overnight.sh

# ── Cloudflare quick-tunnel (ephemeral, random URL per restart) ──────────────
tunnel-url.txt

# ── Docker ───────────────────────────────────────────────────────────────────
postgres_data/

# ── Local agent artifacts ────────────────────────────────────────────────────
.local/

# ── Personal local-only notes / skills (not part of the project) ─────────────
docs/interview-architecture.html
docs/sakura-cloudflare-solution.html
skills/

# ── AWS manual deploy scratch files (contain real account ARNs) ──────────────
backend-task-def.json
backend-taskdef.json
frontend-task-def.json
frontend-taskdef.json
task-done.json
ingress-rules.txt

# ── Personal AWS deploy resume notes (contains real account/VPC/RDS IDs) ─────
docs/aws-deploy-progress.md

# ── Personal portfolio/LinkedIn drafts (not project docs) ─────────────────────
docs/portfolio-pivot-notes.md

# ── Excalidraw diagram experiments (local reference only, not canonical) ─────
docs/diagrams/*.excalidraw
docs/diagrams/rendered/*.svg
docs/diagrams/rendered/*.png
docs/diagrams/rendered-opencode/
