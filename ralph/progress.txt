## Codebase Patterns
- Backend uses Gradle (NOT Maven despite PRD docs), Java 21, Spring Boot 3.4.4
- Flyway migrations in: backend/src/main/resources/db/migration/
- Migration naming: V{n}__{description}.sql (e.g. V1__create_manga_table.sql)
- application.properties uses env var fallbacks: ${DB_URL:jdbc:postgresql://localhost:5432/manga_tracker}
- ddl-auto=validate (JPA validates against Flyway schema at startup)
- Java/JVM is NOT installed in the devcontainer — typecheck verification is by inspection
- Docker is NOT available — Testcontainers integration tests cannot run locally
- Checkstyle config: backend/config/checkstyle/checkstyle.xml
- Scraper layer in package com.mangaTracker.backend.scraper; all scrapers registered as @Component and injected as List<MangaScraper> into ScraperRegistry
- Jsoup dependency already present in build.gradle (org.jsoup:jsoup:1.18.3); no pom.xml in this project
- SakuraMangasScraper uses multi-selector fallback pattern (try multiple CSS selectors in order); selectors may need updating if sakuramangas.org HTML changes

---

## 2026-03-01 - US-001
- What was implemented: Created V1__create_manga_table.sql with manga table (UUID PK, VARCHAR title, TEXT source_url UNIQUE, INTEGER current/latest_chapter, BOOLEAN notifications_enabled, TIMESTAMP columns). Added explicit CREATE UNIQUE INDEX on source_url. Removed V1__create_initial_schema.sql (which had all 3 tables).
- Files changed:
  - backend/src/main/resources/db/migration/V1__create_manga_table.sql (created)
  - backend/src/main/resources/db/migration/V1__create_initial_schema.sql (deleted)
  - ralph/prd.json (US-001 passes set to true)
- **Learnings for future iterations:**
  - The old V1__create_initial_schema.sql had all 3 tables (manga, notification_log, app_settings) — US-002 needs to create V2 and V3 for notification_log and app_settings
  - JVM not available in sandbox, so `./gradlew compileJava` cannot run; typecheck verified by inspection
  - Docker not available, so Testcontainers-based tests cannot run
  - PostgreSQL allows both a UNIQUE constraint and a separate CREATE UNIQUE INDEX on the same column (redundant but valid)
  - Timestamps: AC says TIMESTAMP, old file used TIMESTAMPTZ — new file uses TIMESTAMP to match AC exactly
---

## 2026-03-01 - US-003
- What was implemented: Created the full scraper layer in package com.mangaTracker.backend.scraper
  - MangaScraper interface: supports(String url) and scrape(String url) throws ScrapingException
  - ScrapedManga record: title (String), latestChapter (int)
  - ScrapingException: RuntimeException with message and cause constructors
  - UnsupportedSourceException: RuntimeException thrown by ScraperRegistry when no scraper found
  - ScraperRegistry @Component: injects List<MangaScraper> via constructor, resolve(url) returns matching scraper
  - SakuraMangasScraper @Component: supports sakuramangas.org URLs; Jsoup fetch with 15s timeout; 3 retries with exponential backoff (1s, 2s, 4s); multi-selector fallback for title and chapter extraction; regex CHAPTER_PATTERN extracts integer chapter numbers
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/scraper/MangaScraper.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScrapedManga.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScrapingException.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/UnsupportedSourceException.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScraperRegistry.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/SakuraMangasScraper.java (created)
  - ralph/prd.json (US-003 passes set to true)
- **Learnings for future iterations:**
  - Jsoup was already in build.gradle before this story; no build.gradle change needed
  - Spring automatically collects all @Component beans implementing an interface into a List<InterfaceType> when injected — no manual registration needed
  - SakuraMangasScraper CSS selectors (TITLE_SELECTORS, CHAPTER_SELECTORS) are best-guess patterns for WordPress manga themes; may need updating for the real sakuramangas.org HTML
  - Thread.currentThread().interrupt() must be called before re-throwing when catching InterruptedException
  - Empty catch block (for NumberFormatException fallback) needs a comment to satisfy coding standards

---

## 2026-03-01 - US-002
- What was implemented: Created V2__create_notification_log_table.sql and V3__create_app_settings_table.sql Flyway migrations
  - V2: notification_log table with UUID PK, manga_id FK (ON DELETE CASCADE), chapter_number, sent_at; UNIQUE constraint + index on (manga_id, chapter_number)
  - V3: app_settings table with INTEGER PK (id=1), CHECK constraint enforcing single row, email_notifications_enabled, notification_email, poll_interval_minutes; seed INSERT included
- Files changed:
  - backend/src/main/resources/db/migration/V2__create_notification_log_table.sql (created)
  - backend/src/main/resources/db/migration/V3__create_app_settings_table.sql (created)
  - ralph/prd.json (US-002 passes set to true)
- **Learnings for future iterations:**
  - app_settings uses INTEGER PK (not UUID) because it's a single-row config table — id is always 1
  - The ON DELETE CASCADE on notification_log.manga_id FK means deleting a manga automatically cleans up its notification logs (important for MangaService.deleteManga)
  - app_settings seed row is inserted in the V3 migration itself; no separate seed migration needed
  - Both UNIQUE constraint and UNIQUE INDEX created (same pattern as V1 for consistency)
---
