## 2026-03-02 - US-017
- What was implemented: Unit tests for service layer — tests were already written alongside implementation of US-004 (MangaServiceTest), US-006/US-007 (NotificationServiceTest), and US-005 (SettingsServiceTest); this story marks them as complete
- Files verified (no changes needed):
  - backend/src/test/java/com/mangaTracker/backend/service/MangaServiceTest.java (9 tests: addManga happy path, DuplicateMangaException, UnsupportedSourceException, listManga order, updateManga chapter/notifications, IllegalArgumentException on bad bounds, MangaNotFoundException, deleteManga happy path, MangaNotFoundException)
  - backend/src/test/java/com/mangaTracker/backend/service/NotificationServiceTest.java (5 tests: skip when notificationsDisabled, skip when newChapter not > current, skip when global email disabled, skip when already notified, send+save when all conditions met)
  - backend/src/test/java/com/mangaTracker/backend/service/SettingsServiceTest.java (7 tests: getSettings returns existing, creates default, updateSettings for each field, throws on blank email, throws on zero pollInterval, no-op when all null)
  - ralph/prd.json (US-017 passes set to true)
- **Learnings for future iterations:**
  - All three service test classes use @ExtendWith(MockitoExtension.class) with @Mock and @InjectMocks — pure unit tests, no Spring context or DB needed
  - NotificationServiceTest uses ReflectionTestUtils.setField() to inject @Value("${app.notification.from-email}") since Mockito cannot inject @Value fields automatically
  - CI (GitHub Actions) is authoritative for test pass/fail; unit tests all passed on the most recent CI run
---

## 2026-03-02 - US-016
- What was implemented: Unit tests for scraper layer — tests were already written alongside US-004 implementation; this story marks them as complete
- Files verified (no changes needed):
  - backend/src/test/java/com/mangaTracker/backend/scraper/SakuraMangasScraperTest.java (7 tests: supports() true/false/null, scrape() title+chapter extraction, scrape() missing title/chapter, scrape() network failure)
  - backend/src/test/java/com/mangaTracker/backend/scraper/ScraperRegistryTest.java (3 tests: resolve() matching, resolve() unknown, resolve() empty list)
  - ralph/prd.json (US-016 passes set to true)
- **Learnings for future iterations:**
  - SakuraMangasScraper has two package-private constructors for testability: `(DocumentLoader)` and `(DocumentLoader, long baseDelayMs)` — the second lets tests pass 0ms delay to skip backoff waits during network-failure tests
  - Both test classes live in the same package as the source (`com.mangaTracker.backend.scraper`), allowing access to package-private constructors
  - `spring-boot-starter-test` transitively includes JUnit 5, Mockito, and AssertJ — no extra test dependencies needed
---

## 2026-03-02 - US-015
- What was implemented: Notification preferences settings page — Angular routing added (app.routes.ts, provideRouter); global navbar with "Reading List" and "Settings" nav links (routerLink, routerLinkActive); SettingsComponent with form for emailNotificationsEnabled toggle, notificationEmail input, pollIntervalMinutes input, save button with success/error feedback, loads on init via SettingsService.getSettings(); per-manga notification toggle checkbox added to dashboard reading list table calling MangaService.updateManga(id, { notificationsEnabled }) with immediate UI update on success
- Files changed:
  - frontend/src/app/app.routes.ts (created — routes for / → DashboardComponent, /settings → SettingsComponent)
  - frontend/src/app/app.config.ts (updated — added provideRouter(routes))
  - frontend/src/app/app.component.ts (updated — removed DashboardComponent, added RouterOutlet/RouterLink/RouterLinkActive)
  - frontend/src/app/app.component.html (updated — added <nav> with brand + nav links, <router-outlet />)
  - frontend/src/app/app.component.scss (updated — navbar dark bg, active link underline, .app-main min-height)
  - frontend/src/app/settings/settings.component.ts (created — standalone, inject(SettingsService), form state, loadSettings/onSave)
  - frontend/src/app/settings/settings.component.html (created — @if loading/error/else form, toggle/email/poll inputs, save button, success/error messages)
  - frontend/src/app/settings/settings.component.scss (created — card form layout, field styles, toggle checkbox, save button)
  - frontend/src/app/dashboard/dashboard.component.ts (updated — togglingNotifications map, onNotificationsToggle() handler)
  - frontend/src/app/dashboard/dashboard.component.html (updated — Notify column header + notify-checkbox per row)
  - frontend/src/app/dashboard/dashboard.component.scss (updated — .notify-cell, .notify-checkbox styles)
  - ralph/prd.json (US-015 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - Angular routing: add `provideRouter(routes)` to app.config.ts providers; import `RouterOutlet`, `RouterLink`, `RouterLinkActive` in the root AppComponent standalone imports
  - `[routerLinkActiveOptions]="{ exact: true }"` on the home route link prevents it from being active on all sub-routes
  - For checkbox toggles (uncontrolled pattern), use `[checked]="manga.notificationsEnabled"` + `(change)="handler(manga, $event)"` — same pattern as chapter input; reset `checkbox.checked = manga.notificationsEnabled` in error callback
  - SCSS budget warning at 2kB, error at 4kB — settings.component.scss is 2.14kB (just over warning), dashboard is 2.72kB — both are warnings only, not errors
  - `ng build` + `ng lint` both pass with no errors
---

## 2026-03-02 - US-014
- What was implemented: Delete-manga UI — each manga row now has a red "Delete" button; clicking it shows a browser `window.confirm()` dialog; on confirm, calls `MangaService.deleteManga(id)` and filters the row out of `mangaList` in-place; on cancel, no action taken; disabled state during deletion shown as "Deleting…"; server errors shown in the existing `updateError` map inline below the chapter input
- Files changed:
  - frontend/src/app/dashboard/dashboard.component.ts (added `deletingState` map, `onDelete()` method)
  - frontend/src/app/dashboard/dashboard.component.html (added "Actions" `<th>`, `<td>` with delete button)
  - frontend/src/app/dashboard/dashboard.component.scss (added `.actions-cell`, `.delete-btn` styles)
  - ralph/prd.json (US-014 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - `window.confirm()` is the simplest confirmation dialog — no imports or custom component needed; returns boolean synchronously
  - Filtering the list with `this.mangaList = this.mangaList.filter((m) => m.id !== manga.id)` creates a new array reference, triggering Angular change detection and removing the row
  - Reusing `updateError` map for delete errors works because errors appear inline in the same row; no need for a separate error map
  - SCSS budget warning fires at 2kB (warning) and 4kB (error) per component; dashboard.component.scss is now ~2.56kB — still a warning, not an error
---

## 2026-03-02 - US-013
- What was implemented: Update-reading-progress UI — each manga row in the dashboard now shows a number input for current chapter instead of static text; client-side validation (0..latestChapter), API call on blur/change, inline saving indicator, and per-row error message on failure
- Files changed:
  - frontend/src/app/dashboard/dashboard.component.ts (added savingState/updateError maps, onChapterChange() handler)
  - frontend/src/app/dashboard/dashboard.component.html (replaced static chapter text with `<input type="number">`, saving indicator, error display)
  - frontend/src/app/dashboard/dashboard.component.scss (added .chapter-cell, .chapter-control, .chapter-input, .chapter-saving, .chapter-error styles)
  - ralph/prd.json (US-013 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - Use `[value]="manga.currentChapter"` + `(change)="handler(manga, $event)"` for inputs that trigger API calls on blur — avoids calling API on every keystroke (no NgModel needed; no FormsModule import required)
  - Mutating `manga.currentChapter` directly on the list object (since objects are by reference) triggers Angular change detection and updates `[value]` binding automatically
  - `Record<string, boolean>` and `Record<string, string | null>` work well as per-row state maps keyed by manga.id
  - The anyComponentStyle budget is a warning at 2kB, error at 4kB — dashboard SCSS is now ~2.25kB (just over the warning threshold, safely under error threshold)
  - `input.value = String(manga.currentChapter)` in error callbacks resets the DOM input to the previous value (safe even if element is detached from DOM)
---

## 2026-03-02 - US-012
- What was implemented: AddMangaFormComponent with URL input, loading state, success confirmation, and descriptive error messages (400/409/422); integrated into DashboardComponent; loadManga() made public for refresh on add
- Files changed:
  - frontend/src/app/add-manga/add-manga-form.component.ts (created — standalone, inject(MangaService), @Output mangaAdded EventEmitter, onSubmit() with HTTP error mapping)
  - frontend/src/app/add-manga/add-manga-form.component.html (created — form with ngModel url input, submit button with spinner, error/success messages)
  - frontend/src/app/add-manga/add-manga-form.component.scss (created — card layout, url-input, submit-btn with disabled/hover states, form-error/form-success alerts)
  - frontend/src/app/dashboard/dashboard.component.ts (updated — imports AddMangaFormComponent, loadManga() made public)
  - frontend/src/app/dashboard/dashboard.component.html (updated — added <app-add-manga-form (mangaAdded)="loadManga()" /> above the list)
  - ralph/prd.json (US-012 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - `FormsModule` from `@angular/forms` needed for `[(ngModel)]` in template-driven forms; must be in `imports: []` of standalone component
  - `@Output()` decorator with `EventEmitter` is the correct Angular pattern for child-to-parent events; not flagged by `@angular-eslint/prefer-inject` (only constructor injection is flagged)
  - HTTP error status mapping: use `HttpErrorResponse` from `@angular/common/http` as the type for `error` callback parameter; `err.status` gives the HTTP status code
  - `name` attribute required on `<input [(ngModel)]>` in template-driven forms, otherwise Angular throws a runtime error
  - Making `loadManga()` public on DashboardComponent allows child components (via event binding) to trigger a data refresh without needing a shared service
---

## 2026-03-02 - US-011
- What was implemented: Angular DashboardComponent displaying the reading list with loading state, unread-chapter highlighting, and error handling
- Files changed:
  - frontend/src/app/dashboard/dashboard.component.ts (created — standalone component, inject(MangaService), ngOnInit loads manga, hasUnreadChapters helper)
  - frontend/src/app/dashboard/dashboard.component.html (created — @if/@for control flow, table with title/current/latest/source columns, unread-badge, loading-state, error-state, empty-state)
  - frontend/src/app/dashboard/dashboard.component.scss (created — table styles, .has-unread row highlight, .unread-badge purple pill, loading spinner animation)
  - frontend/src/app/app.component.ts (updated — imports DashboardComponent, removed unused title property)
  - frontend/src/app/app.component.html (updated — replaced Angular default placeholder with <app-dashboard />)
  - ralph/prd.json (US-011 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - Angular 18 built-in control flow (@if, @for) requires NO imports — works in standalone components with empty `imports: []`
  - Self-closing component tags work in Angular 18 templates: `<app-dashboard />` is valid
  - `loadManga()` kept private — called only from ngOnInit for this story; future stories (US-012/013) that need to refresh the list should make it public or add a dedicated `refresh()` method
  - `[href]` binding with external URLs from DB is safe for http/https — Angular strips dangerous protocols; `rel="noopener noreferrer"` added for security on target="_blank" links
  - `ng build` (esbuild) doubles as typecheck — fast and reliable; `ng lint` checks ESLint + Prettier in one command
---

## 2026-03-01 - US-010
- What was implemented: Angular HTTP services for manga and settings API
- Files changed:
  - frontend/src/environments/environment.ts (created — apiUrl: http://localhost:8080, production: false)
  - frontend/src/environments/environment.prod.ts (created — apiUrl: http://localhost:8080, production: true)
  - frontend/src/app/services/manga.service.ts (created — getManga, addManga, updateManga, deleteManga via HttpClient)
  - frontend/src/app/services/settings.service.ts (created — getSettings, updateSettings via HttpClient)
  - frontend/src/app/app.config.ts (updated — added provideHttpClient())
  - frontend/angular.json (updated — added fileReplacements for production build to use environment.prod.ts)
  - ralph/prd.json (US-010 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - Angular 18 ESLint config uses `@angular-eslint/prefer-inject` rule — must use `inject()` function, not constructor parameter injection
  - Prettier config: printWidth=100, singleQuote=true, trailingComma=all — providers array fits on one line at 100 chars; multiline triggers prettier error
  - Environment file replacements go in the `production` build configuration under `fileReplacements` in angular.json (same as older Angular versions)
  - `provideHttpClient()` is the Angular 16+ standalone API (no NgModule needed); replaces `HttpClientModule`
  - Services use `inject()` pattern: `private readonly http = inject(HttpClient)` — cleaner and passes lint
  - The angular.json build system uses `@angular-devkit/build-angular:application` (new esbuild builder), not the old webpack builder
---

## 2026-03-01 - US-009
- What was implemented: SettingsController @RestController with GET /api/settings and PUT /api/settings endpoints
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/controller/SettingsController.java (created)
  - ralph/prd.json (US-009 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - SettingsController follows exact same pattern as MangaController: inner record for request body, direct return of entity (Spring infers 200)
  - PUT (not PATCH) is correct for settings — full replacement semantics; all fields are optional (null-safe in SettingsService)
  - IllegalArgumentException → 400 is already in GlobalExceptionHandler, so validation failures from SettingsService flow through automatically
  - No separate DTO file needed — inner record UpdateSettingsRequest in the controller is sufficient
---

## 2026-03-01 - US-008
- What was implemented: MangaController @RestController with GET/POST/PATCH/DELETE endpoints for /api/manga; GlobalExceptionHandler @RestControllerAdvice mapping domain exceptions to HTTP status codes
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/controller/MangaController.java (created)
  - backend/src/main/java/com/mangaTracker/backend/controller/GlobalExceptionHandler.java (created)
  - ralph/prd.json (US-008 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - MangaController uses inner record types for request bodies (AddMangaRequest, PatchMangaRequest) — no separate DTO files needed; Jackson handles record deserialization in Spring Boot 3
  - Exception → HTTP status mappings: MangaNotFoundException→404, DuplicateMangaException→409, UnsupportedSourceException→400, ScrapingException→422, IllegalArgumentException→400
  - @RestControllerAdvice in the same `controller` package is auto-detected by Spring; no registration needed
  - GET and PATCH return entity directly (Spring infers 200); POST wraps in ResponseEntity.status(CREATED).body(); DELETE returns ResponseEntity.noContent().build() for 204
  - No explicit `produces = "application/json"` needed — @RestController defaults to JSON for all endpoints
---

## 2026-03-01 - US-007
- What was implemented: ScrapingJob @Component in new `job` package — @Scheduled method polls all manga on a configurable interval, scrapes each URL, updates latestChapter + lastCheckedAt in DB, calls NotificationService.notify() on new chapters, catches and logs per-manga failures while continuing
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/job/ScrapingJob.java (created)
  - ralph/prd.json (US-007 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - @EnableScheduling was already present in BackendApplication — no change needed there
  - SpEL in @Scheduled: `@Scheduled(fixedDelayString = "#{${app.scraper.poll-interval-minutes:30} * 60000}")` converts minutes to ms at startup
  - DO NOT use @Transactional on pollAllManga() — without an outer transaction, each mangaRepository.save() and notificationService.notify() runs in its own transaction; if notify() throws and its transaction rolls back, the subsequent save(lastCheckedAt) in the catch block still works in a fresh transaction
  - ConstantName Checkstyle rule requires private static final Logger to be named LOG (not log)
  - MangaRepository.findAllByOrderByUpdatedAtDesc() returns detached entities when called outside a @Transactional context; Spring Data's save() calls merge() on detached entities — this pattern works cleanly for batch job polling
---

## 2026-03-01 - US-006
- What was implemented: NotificationLog JPA entity, NotificationLogRepository, NotificationService (notify method with deduplication and email sending)
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/model/NotificationLog.java (created)
  - backend/src/main/java/com/mangaTracker/backend/repository/NotificationLogRepository.java (created)
  - backend/src/main/java/com/mangaTracker/backend/service/NotificationService.java (created)
  - ralph/prd.json (US-006 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - NotificationLog stores mangaId as plain UUID column (no @ManyToOne mapping) — sufficient for deduplication queries
  - notify() guard order: manga.notificationsEnabled → newLatestChapter > currentChapter → settings.emailNotificationsEnabled → dedup check → send → save log
  - spring-boot-starter-mail and Mailhog config (port 1025) were already present in build.gradle and application.properties from earlier setup
  - app.notification.from-email already in application.properties; TO address comes from AppSettings.notificationEmail (dynamic, from DB)
  - @CreationTimestamp on sentAt handles the NOT NULL sent_at column (no DB default needed)
---

## 2026-03-01 - US-005
- What was implemented: AppSettings JPA entity, AppSettingsRepository, SettingsService (getSettings/updateSettings)
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/model/AppSettings.java (created)
  - backend/src/main/java/com/mangaTracker/backend/repository/AppSettingsRepository.java (created)
  - backend/src/main/java/com/mangaTracker/backend/service/SettingsService.java (created)
  - ralph/prd.json (US-005 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - AppSettings uses Integer PK (not UUID) — always id=1; no @GeneratedValue needed
  - getSettings() is NOT @Transactional(readOnly=true) because it may write a default row if missing
  - updateSettings() accepts nullable params and only updates fields that are non-null (same partial-update pattern as MangaService.updateManga)
  - notificationEmail validated as non-blank when provided; pollIntervalMinutes validated as > 0 when provided
  - IllegalArgumentException used for validation errors (controller layer will map to HTTP 400)
  - The V3 migration seeds the default row, so createDefaultSettings() is a defensive fallback
---

## Codebase Patterns
- Backend uses Gradle (NOT Maven despite PRD docs), Java 21, Spring Boot 3.4.4
- Flyway migrations in: backend/src/main/resources/db/migration/
- Migration naming: V{n}__{description}.sql (e.g. V1__create_manga_table.sql)
- application.properties uses env var fallbacks: ${DB_URL:jdbc:postgresql://localhost:5432/manga_tracker}
- ddl-auto=validate (JPA validates against Flyway schema at startup)
- Java/JVM is NOT installed in the devcontainer — typecheck verification is by inspection
- Docker is NOT available — Testcontainers integration tests cannot run locally
- Checkstyle config: backend/config/checkstyle/checkstyle.xml
- Scraper layer in package com.mangaTracker.backend.scraper; all scrapers registered as @Component and injected as List<MangaScraper> into ScraperRegistry
- Jsoup dependency already present in build.gradle (org.jsoup:jsoup:1.18.3); no pom.xml in this project
- SakuraMangasScraper uses multi-selector fallback pattern (try multiple CSS selectors in order); selectors may need updating if sakuramangas.org HTML changes
- Exception classes live in com.mangaTracker.backend.exception package
- JPA entities use Lombok: @Entity @Table @Getter @Setter @Builder @NoArgsConstructor @AllArgsConstructor
- Use jakarta.persistence (NOT javax.persistence) — Spring Boot 3 uses Jakarta EE 10
- Hibernate @CreationTimestamp/@UpdateTimestamp for created_at/updated_at columns
- CI runs spotlessApply (auto-formats) before checkstyle; minor formatting issues are auto-fixed and committed by CI

---

## 2026-03-01 - US-001
- What was implemented: Created V1__create_manga_table.sql with manga table (UUID PK, VARCHAR title, TEXT source_url UNIQUE, INTEGER current/latest_chapter, BOOLEAN notifications_enabled, TIMESTAMP columns). Added explicit CREATE UNIQUE INDEX on source_url. Removed V1__create_initial_schema.sql (which had all 3 tables).
- Files changed:
  - backend/src/main/resources/db/migration/V1__create_manga_table.sql (created)
  - backend/src/main/resources/db/migration/V1__create_initial_schema.sql (deleted)
  - ralph/prd.json (US-001 passes set to true)
- **Learnings for future iterations:**
  - The old V1__create_initial_schema.sql had all 3 tables (manga, notification_log, app_settings) — US-002 needs to create V2 and V3 for notification_log and app_settings
  - JVM not available in sandbox, so `./gradlew compileJava` cannot run; typecheck verified by inspection
  - Docker not available, so Testcontainers-based tests cannot run
  - PostgreSQL allows both a UNIQUE constraint and a separate CREATE UNIQUE INDEX on the same column (redundant but valid)
  - Timestamps: AC says TIMESTAMP, old file used TIMESTAMPTZ — new file uses TIMESTAMP to match AC exactly
---

## 2026-03-01 - US-004
- What was implemented: Manga JPA entity, MangaRepository, MangaService (addManga, listManga, updateManga, deleteManga), DuplicateMangaException, MangaNotFoundException. Also refactored US-003: moved exceptions to exception package, added injectable DocumentLoader to SakuraMangasScraper, added unit tests for scraper and service layers.
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/model/Manga.java (created)
  - backend/src/main/java/com/mangaTracker/backend/repository/MangaRepository.java (created)
  - backend/src/main/java/com/mangaTracker/backend/service/MangaService.java (created)
  - backend/src/main/java/com/mangaTracker/backend/exception/DuplicateMangaException.java (created)
  - backend/src/main/java/com/mangaTracker/backend/exception/MangaNotFoundException.java (created)
  - backend/src/main/java/com/mangaTracker/backend/exception/ScrapingException.java (created, moved from scraper pkg)
  - backend/src/main/java/com/mangaTracker/backend/exception/UnsupportedSourceException.java (created, moved from scraper pkg)
  - backend/src/main/java/com/mangaTracker/backend/scraper/MangaScraper.java (updated import)
  - backend/src/main/java/com/mangaTracker/backend/scraper/SakuraMangasScraper.java (refactored: injectable DocumentLoader, fixed retry to 4 total attempts)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScraperRegistry.java (updated import)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScrapingException.java (deleted — moved to exception pkg)
  - backend/src/main/java/com/mangaTracker/backend/scraper/UnsupportedSourceException.java (deleted — moved to exception pkg)
  - backend/src/test/java/com/mangaTracker/backend/scraper/SakuraMangasScraperTest.java (created)
  - backend/src/test/java/com/mangaTracker/backend/scraper/ScraperRegistryTest.java (created)
  - backend/src/test/java/com/mangaTracker/backend/service/MangaServiceTest.java (created)
  - ralph/prd.json (US-004 passes set to true)
  - ralph/progress.txt (updated)
- **Learnings for future iterations:**
  - MangaService.addManga: resolve scraper first (UnsupportedSourceException), then check duplicate (DuplicateMangaException), then scrape (ScrapingException), then save
  - MangaService.updateManga: throws IllegalArgumentException for invalid currentChapter bounds (controller maps this to HTTP 400)
  - ON DELETE CASCADE in V2 migration means deleteManga just needs deleteById — no manual cascade needed
  - @Transactional on service class, @Transactional(readOnly=true) on read-only methods
  - SakuraMangasScraper: DocumentLoader interface is nested + package-private; tests in same package can use it directly as lambda target
---

## 2026-03-01 - US-003
- What was implemented: Created the full scraper layer in package com.mangaTracker.backend.scraper
  - MangaScraper interface: supports(String url) and scrape(String url) throws ScrapingException
  - ScrapedManga record: title (String), latestChapter (int)
  - ScrapingException: RuntimeException with message and cause constructors
  - UnsupportedSourceException: RuntimeException thrown by ScraperRegistry when no scraper found
  - ScraperRegistry @Component: injects List<MangaScraper> via constructor, resolve(url) returns matching scraper
  - SakuraMangasScraper @Component: supports sakuramangas.org URLs; Jsoup fetch with 15s timeout; 3 retries with exponential backoff (1s, 2s, 4s); multi-selector fallback for title and chapter extraction; regex CHAPTER_PATTERN extracts integer chapter numbers
- Files changed:
  - backend/src/main/java/com/mangaTracker/backend/scraper/MangaScraper.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScrapedManga.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScrapingException.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/UnsupportedSourceException.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/ScraperRegistry.java (created)
  - backend/src/main/java/com/mangaTracker/backend/scraper/SakuraMangasScraper.java (created)
  - ralph/prd.json (US-003 passes set to true)
- **Learnings for future iterations:**
  - Jsoup was already in build.gradle before this story; no build.gradle change needed
  - Spring automatically collects all @Component beans implementing an interface into a List<InterfaceType> when injected — no manual registration needed
  - SakuraMangasScraper CSS selectors (TITLE_SELECTORS, CHAPTER_SELECTORS) are best-guess patterns for WordPress manga themes; may need updating for the real sakuramangas.org HTML
  - Thread.currentThread().interrupt() must be called before re-throwing when catching InterruptedException
  - Empty catch block (for NumberFormatException fallback) needs a comment to satisfy coding standards

---

## 2026-03-01 - US-002
- What was implemented: Created V2__create_notification_log_table.sql and V3__create_app_settings_table.sql Flyway migrations
  - V2: notification_log table with UUID PK, manga_id FK (ON DELETE CASCADE), chapter_number, sent_at; UNIQUE constraint + index on (manga_id, chapter_number)
  - V3: app_settings table with INTEGER PK (id=1), CHECK constraint enforcing single row, email_notifications_enabled, notification_email, poll_interval_minutes; seed INSERT included
- Files changed:
  - backend/src/main/resources/db/migration/V2__create_notification_log_table.sql (created)
  - backend/src/main/resources/db/migration/V3__create_app_settings_table.sql (created)
  - ralph/prd.json (US-002 passes set to true)
- **Learnings for future iterations:**
  - app_settings uses INTEGER PK (not UUID) because it's a single-row config table — id is always 1
  - The ON DELETE CASCADE on notification_log.manga_id FK means deleting a manga automatically cleans up its notification logs (important for MangaService.deleteManga)
  - app_settings seed row is inserted in the V3 migration itself; no separate seed migration needed
  - Both UNIQUE constraint and UNIQUE INDEX created (same pattern as V1 for consistency)
---
