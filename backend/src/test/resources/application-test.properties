# Overrides for integration tests (Testcontainers provides the real values at runtime)
spring.flyway.enabled=true
spring.jpa.hibernate.ddl-auto=validate

spring.mail.host=localhost
spring.mail.port=1025

app.scraper.poll-interval-minutes=1
app.notification.from-email=tracker@test.local
app.notification.to-email=test@test.local
